version: 0.2
phases:
  install:
    commands:
      - echo "Install phase"
  build:
    commands:
      - echo "Build phase"
  post_build:
    commands:
      - echo "Post build phase"
# artifacts: 섹션도 잠시 제거하거나 최소화
artifacts:
  files:
    - README.md # 임시로 최소한의 파일 지정